#!/bin/bash
# validing--Validates integer input, allowing negative integers too

validint()
{
    # Validate first fields and test that value against min value $2 and/or
    #  max value $3 if they are supplied.  If the value isn't within range
    #  or it's not composed of just digits, fail.

    number
}